services:
  fastmoss-report:
    image: nginx:alpine
    container_name: fastmoss-report
    restart: unless-stopped
    ports:
      - "8090:80"
    volumes:
      - ./orbstack-www/ning_mac:/usr/share/nginx/html:ro
      - ./nginx.fastmoss.conf:/etc/nginx/conf.d/default.conf:ro
